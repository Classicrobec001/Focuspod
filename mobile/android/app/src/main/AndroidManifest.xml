<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- react-native-track-player: foreground service for background audio -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <!-- Keeps the CPU running while audio plays in the background -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- IpodWheel haptic feedback during rotary drag -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- App blocking: usage stats to detect foreground app -->
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />
    <!-- File downloads to internal app storage.
         Required on Android ≤ 9 (API 28); harmless on API 29+ where scoped storage
         makes this permission optional for app-private directories. -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:supportsRtl="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>

      <!--
        react-native-track-player foreground service.
        Explicitly declared here (in addition to the auto-merge from RNTP's own
        AndroidManifest.xml) so that:
          • android:foregroundServiceType="mediaPlayback" is guaranteed in the final
            APK — Android 10+ silently kills media playback foreground services that
            lack this attribute.
          • WAKE_LOCK is held for the duration of playback so the CPU stays awake.
          • tools:node="merge" lets Gradle merge our declaration with RNTP's rather
            than treating them as a conflict.
      -->
      <service
          android:name="com.doublesymmetry.trackplayer.service.MusicService"
          android:enabled="true"
          android:exported="true"
          android:foregroundServiceType="mediaPlayback"
          tools:node="merge">
          <intent-filter>
              <action android:name="android.intent.action.MEDIA_BUTTON" />
          </intent-filter>
      </service>
      <!--
        FocusPod Accessibility Service — used for real-time foreground-app
        detection during focus sessions.  TYPE_WINDOW_STATE_CHANGED events
        fire instantly (vs. the UsageStatsManager polling fallback which can
        lag 2-5 s on emulators / Android 10+ devices).

        The user must enable this manually under:
          Settings → Accessibility → Installed apps → FocusPod
      -->
      <service
          android:name=".blocking.FocusPodAccessibilityService"
          android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
          android:exported="true"
          android:label="@string/app_name">
          <intent-filter>
              <action android:name="android.accessibilityservice.AccessibilityService" />
          </intent-filter>
          <meta-data
              android:name="android.accessibilityservice"
              android:resource="@xml/accessibility_service_config" />
      </service>
    </application>
</manifest>
