{
  "name": "@focuspod/core",
  "version": "1.0.0",
  "private": true,
  "description": "Platform-agnostic FocusPod domain layer: types, catalog service, ports and stores.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zustand": "^5.0.12"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
